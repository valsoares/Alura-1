import React from 'react';
import Slide from '../slide';

import {ListTitulo, ListVideo, ListContent} from '../styles';

export default function List() {
    return(
        <div>
            <Slide/>
            {/* <ListTitulo borderColor={'blue'}>Drama</ListTitulo>
                <ListContent>
                    <ListVideo color={'blue'}></ListVideo>
                    <ListVideo color={'blue'}></ListVideo>
                    <ListVideo color={'blue'}></ListVideo>
                </ListContent>
            <ListTitulo borderColor={'#fff'}>Ficção e Fantasia</ListTitulo>
                <ListContent>
                    <ListVideo color={'#fff'}></ListVideo>
                    <ListVideo color={'#fff'}></ListVideo>
                    <ListVideo color={'#fff'}></ListVideo>
                </ListContent>
            <ListTitulo borderColor={'var(--azul)'}>Suspense</ListTitulo>
                <ListContent>
                    <ListVideo color={'var(--azul)'}></ListVideo>
                    <ListVideo color={'var(--azul)'}></ListVideo>
                    <ListVideo color={'var(--azul)'}></ListVideo>
                </ListContent> */}
        </div>
    );
}